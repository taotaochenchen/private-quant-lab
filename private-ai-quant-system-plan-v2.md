
# 基于大模型的私人全自动量化交易系统计划书（全自主决策版）

> 当前实施范围调整：优先“系统给出建议 + 用户人工操作”，暂停实盘接口和 Computer Use 执行推进。以 T-1（上一交易日）的建议为研究基线，结合今日数据与实际执行反馈修订当日策略，展示保留、调整、撤回、新增及风控暂停。下文全自动执行内容保留为历史远期设计，不代表当前运行方式。实现与限制见 [每日策略修订](docs/daily-strategy-revision.md)。

## 1. 项目定位

本项目旨在构建一个面向个人投资者或小型投资团队的**全自动 AI 量化交易系统**。系统每天在开盘前自动完成市场情绪判断、行业方向筛选、个股下钻分析、金融算子评估、风控校验和交易计划生成，并**在无人干预的情况下自动执行订单**，包括买入、加仓、减仓、止损与清仓。

系统核心由三部分组成：

- **大模型 Agent 集群**：负责信息理解、逻辑推理、观点整合与决策生成。
- **确定性金融算子**：负责数据计算、因子评分与风险量化。
- **独立风控与执行层**：拥有硬性否决权，负责资金安全、限额控制、熔断和订单执行。

**重要边界**：系统不是简单的“大模型荐股 + 自动下单”，而是有严格风控约束、可追溯、可熔断的自动交易系统。人只保留紧急干预权和系统级监督权，不参与日常交易决策。

---

## 2. 核心目标

1. 每日开盘前自动完成全市场分析。
2. 自动判断市场方向与交易模式（进攻、防守、轮动、空仓）。
3. 自动筛选候选行业（2~4 个）。
4. 自动下钻行业内个股并进行算子评分。
5. 自动执行风控审核，生成可执行订单。
6. **自动将订单发送至券商接口，无需人工确认。**
7. 盘中自动监控持仓和触发条件，动态调整仓位。
8. 自动进行收盘复盘，优化策略参数。
9. 保留完整证据链和决策日志，支持审计与改进。

---

## 3. 系统运行流程（无人干预）

```text
每日定时启动（开盘前 N 小时）
  ↓
自动数据采集与健康检查
  ↓
市场情绪 Agent 自动分析
  ↓
总控 Agent 决定今日交易模式
  ↓
行业研究 Agents 并行筛选
  ↓
反方观点 Agent 校验
  ↓
个股发现 Agent 下钻
  ↓
金融算子 Agent 计算评分
  ↓
风控 Agent 独立审核（可降仓/否决/熔断）
  ↓
交易计划 Agent 生成订单指令
  ↓
自动交易执行模块发送订单
  ↓
盘中监控 Agent 跟踪触发条件，自动调仓
  ↓
收盘复盘 Agent 自动记录与评价
  ↓
系统自动更新策略状态，等待下一交易日
```

用户角色：只查看系统运行状态、持仓、交易记录和复盘报告，不参与具体交易决策。紧急情况下可通过手动开关暂停系统。

---

## 4. 系统功能模块

### 4.1 盘前自动分析模块
- 定时任务调度，无需人工触发。
- 拉取行情、新闻、资金、公告、宏观数据。
- 数据完整性检查，不完整时自动降级或暂停交易。
- 启动多 Agent 分析流程。
- 输出盘前分析快照和交易模式。

### 4.2 市场情绪分析模块
输出结构包含量化分数和禁则条件（同上修订版），所有禁则条件将直接用于风控模块。

### 4.3 行业深度研究模块
输出行业评分、证据链、反方观点和失效条件，结果自动进入总控 Agent 排序。

### 4.4 个股发现模块
在选定行业内自动筛选股票，过滤条件全部量化，输出候选池。

### 4.5 金融算子评估模块
输出所有算子值和总分，**大模型不得修改任何数字**，只能解释原因。

### 4.6 风控模块（独立，拥有硬性否决权）
风控模块不依赖研究模块，可直接否决或修改交易计划。新增以下自动风控能力：

- 单股、单行业、总仓位自动限制。
- 单日最大亏损熔断（达到阈值自动平仓并禁止开仓）。
- 账户最大回撤熔断。
- 流动性检查（订单数量不超过日成交量的 X%）。
- 高波动环境自动降杠杆或降仓。
- 相关性检查（行业/个股高度相关时自动降低总仓位）。
- 财报、公告、解禁窗口自动禁止开仓。
- 自动设置止损单、止盈单，并发送至券商。

### 4.7 交易计划与自动执行模块
交易计划 Agent 生成结构化订单指令，直接发送给自动交易执行模块。订单指令包含：

- 股票代码、方向、数量、价格类型（限价/市价）、有效期。
- 买入/卖出触发条件（量化）。
- 止损价、止盈价。
- 订单超时处理策略。

自动执行模块与券商接口对接，实现：

- 订单自动提交、撤单、重试。
- 成交回报自动同步。
- 持仓实时同步。
- 部分成交处理。
- 断线重连与订单状态恢复。

### 4.8 盘中监控模块
自动监控所有持仓和候选标的，根据盘前设定的量化条件自动触发：

- 买入、加仓、减仓、清仓。
- 止损、止盈。
- 风控熔断。
- 策略失效条件触发后自动退出。

### 4.9 收盘复盘模块
自动生成每日复盘报告，包括：

- 推荐行业/个股的实际表现。
- 订单执行质量（滑点、成交率）。
- 风控是否有效避免了亏损。
- 策略胜率、盈亏比、最大回撤。
- 自动调整模型参数或算子权重（可选）。

---

## 5. Agent 架构设计（不变，但强调全自动）

总控 Agent、市场情绪 Agent、行业研究 Agent、个股发现 Agent、金融算子 Agent、反方观点 Agent、风控 Agent、交易计划 Agent、盘中监控 Agent、收盘复盘 Agent。所有 Agent 均自动运行，无需人工干预。

**新增：交易执行 Agent**，负责将交易计划转换为实际订单并管理订单生命周期。

---

## 6. 工作流与边界控制

工作流与之前类似，但增加自动执行环节。边界控制继续保留：

- 最大工具调用轮数、超时、重复调用检测、失败降级。
- 所有 tool call 和 tool result 落库。
- 模型不得修改算子输出的数字。
- 交易执行前必须通过风控模块，否则订单不会发出。

---

## 7. 数据架构

在原有基础上增加：

- **订单数据层**：保存所有订单、成交、撤单记录。
- **持仓数据层**：实时同步账户持仓。
- **风控事件层**：记录所有风控触发、熔断、降仓事件。

数据质量要求更严格：任何数据延迟或缺失都可能导致错误交易，因此必须有多源校验和超时处理。

---

## 8. 前端交互设计

用户界面不再是“决策工作台”，而是**系统监控台**：

- 实时显示系统运行状态、持仓、今日交易记录。
- 显示风控状态、熔断状态、可用资金。
- 提供紧急停止按钮（人工干预唯一入口）。
- 可查看历史报告、证据链和日志。

开发者调试面板依然保留，但默认隐藏。

---

## 9. 技术架构建议

新增以下服务：

- **交易执行服务**：对接券商 API，高可用、低延迟。
- **订单管理服务**：订单状态机，处理部分成交、撤单、重试。
- **持仓同步服务**：实时获取账户持仓和资金。
- **风控引擎**：独立进程，实时监控所有持仓和订单，触发熔断立即执行。
- **消息队列**：用于 Agent 之间异步通信。

推荐使用有状态工作流框架（如 Temporal）以保证订单执行的可靠性和可恢复性。

---

## 10. 风险与合规

全自动交易系统风险更高，必须增加以下措施：

- **硬编码熔断**：当日亏损达到阈值，系统自动平仓并停止交易，需人工重置。
- **单日最大交易次数限制**。
- **订单频率限制**，防止异常下单。
- **账户权限分离**：交易账户与系统账户分离，限制最大可亏损金额。
- **模拟盘先行**：实盘前必须经过至少一个月的模拟盘验证。
- **合规审查**：个人账户需遵守券商 API 使用规范，不得进行市场操纵。
- **完整审计日志**：所有决策、订单、风控事件可追溯。
- **紧急停止开关**：可随时手动暂停系统。

---

## 11. MVP 范围

MVP 阶段直接包含**自动下单（模拟盘）**，暂不接实盘。功能包括：

- 每日盘前自动分析。
- 市场情绪评分。
- 行业/个股筛选。
- 金融算子评分。
- 风控审核。
- 自动生成交易计划。
- **模拟盘自动下单执行。**
- 盘中自动监控和调仓（模拟）。
- 收盘复盘。
- 监控台界面。

实盘接入放在第五阶段，且需通过严格测试和人工授权。

---

## 12. 阶段规划

调整如下：

### 第一阶段：契约冻结与模拟盘闭环
- 定义所有 Schema。
- 跑通 mock 数据 + 模拟下单流程。
- 前端监控台原型。

### 第二阶段：真实数据与算子接入
- 接入真实行情数据。
- 建立基础算子库。

### 第三阶段：Agent 编排与自动执行
- 实现多 Agent 工作流。
- 接入模拟券商接口，自动下单。

### 第四阶段：盘中监控与复盘
- 实现实时监控和自动调仓（模拟盘）。
- 复盘 Agent 上线。

### 第五阶段：实盘接入
- 通过严格测试后接入真实券商 API。
- 小资金试运行，逐步放大。

---

## 13. 成功指标

新增：

- 订单执行延迟 < 1 秒。
- 订单成交率 > 95%。
- 风控熔断触发准确率 100%。
- 系统可用性 > 99.5%（交易时段）。
- 无人工干预下连续运行天数。

保留原有研究和产品指标。

---

## 14. 当前工程落地路线

当前已完成能力不变。下一步优先完成：

1. **定义 PreMarketReport 及订单、风控事件 Schema。**
2. **实现模拟盘交易执行模块，对接模拟券商接口。**
3. **前端监控台替代原盘前工作台，展示自动运行状态。**
4. **增加风控引擎独立服务，实时监控模拟持仓。**
5. **跑通全自动模拟盘闭环：分析 → 决策 → 下单 → 监控 → 复盘。**

---

以上计划书已根据“全自动自主决策，包括下单”的要求全面修订。系统从辅助决策工具升级为无人值守自动交易系统，同时大幅强化了风控、熔断、订单执行可靠性和审计能力。
